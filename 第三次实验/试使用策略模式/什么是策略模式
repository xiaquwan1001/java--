策略模式（Strategy Pattern）简介
策略模式是一种行为型设计模式，它允许你在运行时选择不同的算法或策略，而不需要修改调用它的代码。

在优化算法中，策略模式可以用于：

选择不同的优化方法（如梯度下降、模拟退火、遗传算法等）。

动态切换搜索策略（如贪心算法、动态规划等）。

在不同输入规模下使用不同的计算方式（如 O(n) 和 O(log n) 方法）。
核心
策略接口（Strategy）：定义所有策略的通用方法。

具体策略（Concrete Strategy）：不同的算法实现。

上下文（Context）：持有一个策略对象，并在需要时调用策略的方法。

策略模式不仅用于排序算法，还可以用于：

优化搜索算法：自动选择深度优先搜索（DFS）或广度优先搜索（BFS）。

优化路径规划：如 Dijkstra、A* 等路径算法的动态选择。

优化机器学习：自动选择不同的优化方法，如 SGD、Adam、RMSProp。

数据压缩：根据数据类型选择不同的压缩算法，如 Huffman、LZ77 等。

总结
策略模式本质上就是把“算法的选择”从调用逻辑中剥离，让算法可以自由替换。
在优化算法中，策略模式能让代码更灵活、扩展性更强，而且避免硬编码多个 if-else 逻辑。

